<h1>All of the links!</h1>

<h2>Submit your new link here!</h2>

<%= form_for(@link) do |f| %>
  <%= f.label :url %>
  <%= f.text_field :url %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.submit "Submit" %>
<% end %>
